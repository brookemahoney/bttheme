{#
/**
 * @file
 * Theme override for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 */
#}
{% if breadcrumb %}
  {% include "@bttheme/navigation/breadcrumb/breadcrumb.twig" with {title: 'Breadcrumb'|t} %}
{% endif %}
